name: Unit Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions: {}

jobs:
  unit-test:
    uses: sxzz/workflows/.github/workflows/unit-test.yml@vp

  # coverage:
  #   uses: sxzz/workflows/.github/workflows/coverage.yml@vp
  #   needs: unit-test
  #   permissions:
  #     id-token: write
